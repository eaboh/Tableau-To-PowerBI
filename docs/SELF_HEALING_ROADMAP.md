# Self-Healing Roadmap (v3.2 → v3.4)

Status as of Sprint 137 (v31.1.0):
- **20 v3 healers** wired in `powerbi_import/self_healing_v3.py`
- Total grand-suite tests: **7,491 passing**, 55 skipped, 1 xfailed

## ✅ Delivered in Sprints 138 + 139 (v31.2.0)

- **v3.2** — 10 schema/datatype healers (#35–#44) — *DELIVERED*
- **v3.3** — 10 Power Query / M healers (#45–#54) — *DELIVERED*
- Total v3 healers now: **40** (`_V3_HEALERS` tuple)
- Grand-suite tests: **7,542 passing**, 55 skipped, 1 xfailed

## 🚧 Pending — Sprint 140 (v3.4)

The originally-planned PBIR/visual healers were **deferred** because
`self_healing_v3.py` operates exclusively on the TMDL model dict, and the
report layer (PBIR pages, visuals, bookmarks, themes) is generated by a
separate pipeline (`pbip_generator.py` / `visual_generator.py`). A clean
implementation requires a sibling module — proposed name:
`powerbi_import/self_healing_report.py` — wired into `pbip_generator` after
report assembly. This will be tackled as **Sprint 140**.

---

---

## Sprint 138 — v3.2 — *Schema & Datatype hygiene* (10 healers)

Target failures: Vertipaq compression errors, type-coercion errors during refresh,
column lineage errors when opening the model.

| # | Healer | Symptom Fixed | Action |
|---|--------|---------------|--------|
| 35 | `column_without_datatype` | `Cannot determine the data type` | Default to `string` and tag with annotation |
| 36 | `measure_without_datatype` | Measure shown as "variant" in PBI | Infer from expression (SUM→decimal, COUNT→int64) |
| 37 | `boolean_with_string_default` | `false`/`true` stored as text in column default | Convert to lowercase bool literal |
| 38 | `numeric_format_string_mismatch` | `formatString` like `"0.00"` on int64 column | Drop fractional digits or promote dtype |
| 39 | `datetime_without_format` | Date columns rendered as numbers | Add `formatString = "General Date"` |
| 40 | `lineage_tag_collision` | Duplicate `lineageTag` GUID across model | Regenerate uuid4 for the second occurrence |
| 41 | `missing_lineage_tag` | New columns/measures without lineageTag | Inject deterministic uuid5 from "table.column" |
| 42 | `source_column_missing` | `sourceColumn` references non-existent M output | Match by name (case-insensitive); else drop column |
| 43 | `key_column_nullable` | Marked `isKey` but allows nulls | Force `isNullable = false` |
| 44 | `int_column_with_decimal_default` | `defaultValue = 1.5` on int column | Round to nearest int |

**Test target**: ~40 unit tests, ~7,531 grand total.

---

## Sprint 139 — v3.3 — *Power Query / M-partition hygiene* (10 healers)

Target failures: Refresh "Expression.Error", "Token Comma expected", invalid step names.

| # | Healer | Symptom Fixed | Action |
|---|--------|---------------|--------|
| 45 | `m_unbalanced_let_in` | Missing `in` keyword | Append `in <last step name>` |
| 46 | `m_unbalanced_parens` | Refresh fails with parse error | Auto-balance trailing `)` `]` `}` |
| 47 | `m_step_name_collision` | Two steps share a name within `let` | Rename second to `name_2` and rewire refs |
| 48 | `m_invalid_identifier_unquoted` | Step or column names with spaces/specials | Wrap in `#"..."` |
| 49 | `m_trailing_comma_in_record` | `[a=1, b=2,]` → parse error | Strip trailing comma in `[]` and `{}` |
| 50 | `m_double_comma` | `Table.SelectRows(t,, …)` | Collapse `,,` → `,` |
| 51 | `m_missing_source_step` | Partition body uses `Source` but no `Source =` | Insert `Source = …` placeholder + warn |
| 52 | `m_credential_in_expression` | Hardcoded user/password in M | Replace with `#"<placeholder>"` and record severity=high |
| 53 | `m_partition_mode_mismatch` | `mode = "import"` but expression uses DirectQuery functions | Switch to `directQuery` or wrap in Table.Buffer |
| 54 | `m_dataflow_ref_dangling` | `PowerPlatform.Dataflows` ref to deleted query | Replace body with empty `#table` and warn |

**Test target**: ~40 unit tests, ~7,571 grand total.

---

## Sprint 140 — v3.4 — *Report (PBIR) & visual hygiene* (10 healers)

Target failures: Report opens but visuals show "Can't display the visual" or
fail to render. These run **after** TMDL but before PBIP zip.

| # | Healer | Symptom Fixed | Action |
|---|--------|---------------|--------|
| 55 | `visual_field_dangling` | Visual references measure/column removed by earlier healers | Drop the field role; if visual becomes empty, mark hidden |
| 56 | `visual_overlap_offscreen` | x/y > 1280, falls outside canvas | Clamp to page bounds |
| 57 | `visual_zero_size` | width=0 or height=0 | Reset to default 320×240 |
| 58 | `visual_duplicate_name` | Two visuals share `name` GUID | Regenerate the second |
| 59 | `slicer_target_missing` | Sync-slicer points to deleted page | Drop sync target |
| 60 | `bookmark_dangling_visual` | Bookmark refs visual id that no longer exists | Strip the entry |
| 61 | `page_duplicate_ordinal` | Two pages with same `ordinal` | Re-sequence 0..N |
| 62 | `theme_color_invalid_hex` | `#GGGGGG` or wrong length | Replace with default palette color |
| 63 | `filter_field_dangling` | Page/visual filter on dropped field | Remove filter entry |
| 64 | `image_resource_missing` | RegisteredResources ref a missing image file | Replace with placeholder PNG (1×1 transparent) |

**Test target**: ~40 unit tests, ~7,611 grand total.

---

## Cross-cutting work (folded into the 3 sprints)

- **Severity audit pass** — every healer tagged `info|warn|error|high` in RecoveryReport.
- **Idempotency proof** — add a meta-test that runs `run_v3_healers` twice and
  asserts second run repairs `0`.
- **Healer ordering matrix** — document dependencies (e.g. #42 must run *after* #41).
- **CHANGELOG** — version bumps `v31.2.0`, `v31.3.0`, `v31.4.0`.

---

## Stretch (post v3.4) — *RLS & Calculation Group hygiene*

Reserved for **Sprint 141 (v3.5)** if grand-suite stays green:

- RLS role with empty `tablePermissions`
- RLS expression referencing missing column
- Calculation item with empty `formatString` on calc group
- Calc group `precedence` collisions
- Field parameter table with mismatched `nameOf` count
- Perspective references to dropped tables

---

## Acceptance gate per sprint

1. `pytest tests/test_self_healing.py tests/test_self_healing_v3.py` → 100% green
2. Full suite: **zero regressions**, total +~40 each sprint
3. `_V3_HEALERS` count assertion updated
4. Coverage ≥ 96.0% (current floor)
5. CHANGELOG entry with healer table
6. Commit message: `Sprint NNN: Self-Healing v3.X — <theme> (+10 healers)`
